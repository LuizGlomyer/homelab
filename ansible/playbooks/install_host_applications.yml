- name: Install host applications
  hosts: [raspberry]
  become: true
  roles:
    - role: extract_metadata
      tags: extract_metadata

    - role: services/caddy
      tags: caddy

    - role: services/olivetin
      tags: olivetin